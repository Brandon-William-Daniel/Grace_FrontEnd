import React from "react"

const Logout = () => {
    return(
        <div>
            <h3>When a user clicks this link it will sign them out of their account. I dont think this should be an actual page, we should discuss as a group</h3>
        </div>
    )
}

export default Logout